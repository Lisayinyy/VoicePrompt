# 2026-09-15 公网连接故障核查

状态：已找到公网阻断原因，尚未恢复，不得宣布完整修复或提交 0.8.0 市场更新。

最新进展：用户已自行完成广州实例续费。控制台已核实到期时间为 2026-12-11 15:40:15；备案资源校验已不再提示时长不足，显示剩余 5 个可用网站名额。**备案本身仍为未提交草稿，公网 TLS 失败、HTTP 备案拦截仍存在**。已在原草稿填入 voiceprompt.work，主体类型、备案区域与身份信息尚待用户确认/填写，没有提交身份资料或备案申请。下文的“未续费”属于更早的检查记录。

用户随后确认使用个人主体，已在原草稿选择「个人」。页面要求备案区域、适用证件类型、姓名、证件号码及证件照片，目前尚未填写或上传这些身份资料。域名字段已输入 voiceprompt.work，页面提示需先完善主办单位信息再验证域名；未点击下一步或提交备案。当前任务需用户本人在腾讯云填写实际身份信息后继续。

## 实际证据

- 本机连接 `https://api.voiceprompt.work/healthz` 在 TLS 握手阶段失败，Node 报 `ECONNRESET`。
- 同一主机的公网 HTTP 请求返回 302，Location 指向 `https://dnspod.qcloud.com/static/webblock.html?d=api.voiceprompt.work`。没有跟随该跳转，也没有通过 HTTP 发送任何凭证或录音文字。
- 腾讯云当前账号“我的备案”显示“待验证备案－草稿”“提交初审 未提交”，主体及互联网信息服务均显示“暂无备案”。已有草稿尚处于验证备案类型阶段。
- 草稿中的现有轻量服务器提示：购买时长不足 3 个月，需续费后刷新有效期。没有续费、付款或提交备案。
- 续费对话框已选择 2 个月，页面完成报价后显示合计 ¥70，续费后到期时间为 2026-12-11 15:40:15；没有点击“确定”。这是当前页面报价，不是已经购买。
- `voice-prompt` 与 `voice-prompt-caddy` 均运行正常，后端只监听回环地址；服务器本机 HTTPS 返回 200。
- 在服务器本机通过完整证书验证调用真实 MiniMax 润色成功：HTTP 200，`fallback=false`，保留合成测试中的版本 2.0 和数据库约束，用时约 1353 毫秒。这个结果不能代替公网可用性。
- 最新本机诊断显示 build 29 的麦克风和辅助功能权限均已允许。这只能证明授权状态，不能代替真实输入框写入测试。

## 后续前置条件

1. 用户核实是否已有其他账号或接入商下的 ICP 备案号；当前账号未显示已备案记录。域名实名认证与 ICP 备案需要区分。
2. 如需首次备案，现有服务器需满足购买时长条件，随后填写真实主体与服务信息、完成所需验证并提交审核。续费金额与身份资料应由用户确认；不替用户作身份声明。
3. 腾讯云解除公网阻断后，从用户网络重新验证 TLS、自动开通、真实中文/英文润色。
4. 验证本机以及另一台物理 Mac 的安装、授权和 MiniMax Code 输入框填入，确认不自动发送。

发布预检现在每次直接检查真实公网 HTTPS，不能仅依据历史成功 JSON 放行。旧报告只代表其记录时间的测试结果。

此次相关 HTTPS 验证测试 2 项通过；实际发布预检返回 `blocked`，并包含 `public_https_currently_unavailable`、另一台物理 Mac 验收待完成、公开下载验收待完成。未重新构建或安装桌面二进制，避免无必要地再次影响刚恢复的系统授权。

官方依据：
- [域名无法访问网站](https://cloud.tencent.com/document/product/242/53667)
- [备案云资源要求](https://cloud.tencent.com/document/product/243/18908)

后续处理：用户已决定保留广州，不购买香港实例。再次查看腾讯云，备案仍为初审未提交、资源仍提示不足 3 个月；续费两个月报价仍为 70 元，尚未确认购买。后续已在本机构建安装 build 30，修正状态提示并更新两端插件文件；公网问题尚未解决，详见 [build 30 验证记录](free-build30-verification.json)。
