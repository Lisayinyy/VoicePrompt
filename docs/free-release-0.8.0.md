# Voice Prompt 0.8.0 免费版交付状态

用户决策：取消邀请码，默认向所有支持的桌面用户提供免费 AI 润色。每台 Mac 自动生成独立凭证，发布者的 MiniMax Key 只保留在服务器。

2026-09-15 最新决定：保留广州实例，不购买香港服务器，不切换 DNS。完成广州备案并恢复公网后，才进行第二台 Mac 的真实免费润色与 MiniMax Code 自动填入验收。

**当前阻碍（2026-09-15）**：用户已续费，广州实例到期时间为 2026-12-11 15:40:15，备案服务器时长校验已通过。但当前账号备案仍为未提交草稿，公网域名仍被腾讯云备案拦截。续费不等于备案通过，服务器内模型调用成功不代表用户网络可用。详见 [连接故障核查](connection-incident-2026-09-15.md)。下面“已验证”内容均须按报告时间理解。

## 先前验证记录（不代表当前公网可用）

- 广州服务器 HTTPS 已启用，TLS 1.3、公开可信证书，80/443 放行，8787 与管理端口仍只监听回环
- `/client/enroll` 无需邀请码，自动开通；润色仍需独立凭证，陌生浏览器和伪造凭证被拒绝
- 真实中文和英文 MiniMax 润色成功；不会把保真检查失败后的原文回退当作润色成功
- 每台设备每天 30 次调用；服务器沿用每天 1000 次、每月 10000 次、并发 2 的总量上限。达到上限保留原文，不向用户收费
- 桌面 0.8.0 build 28 自动开通后，原生 UI 已观察到“免费 AI 润色已就绪”；不再依赖发布者的本地邀请测试进程
- 全新隔离目录安装与升级保留配置测试已执行。隔离目录不是第二台物理电脑
- 本机 OMP 与 MiniMax 本地插件文件已更新 0.8.0，活跃 MCP 会话需重新连接

## 发布前仍需确认

- 当前 Mac 的新版辅助功能和麦克风授权，以及 MiniMax Code 输入框真实写入、不自动发送
- 另一台 Mac 的完整安装和录音填入结果
- 最终公开下载链接与校验值；市场申请不能引用旧版 0.7.1 包
- 当前构建没有 Developer ID 公证，企业管理策略可能阻止安装。不要关闭系统防护来绕过

市场更新尚未提交。preflight:minimax-submit 对 0.8.0 使用新的完整流程门槛，不能再通过旧版包检查冒充当前版本已验收。

测试报告：public-free-verification.json、free-desktop-verification.json、free-installed-mcp-verification.json

## 2026-09-14 界面修订（build 29）

- 删除设置中的“无需邀请码，无需 API Key，安装后自动开通”说明
- 连接中、连接失败优先于已激活状态；失败时显示“连接失败”和“重新连接”，TLS 错误使用简短中文提示
- 原生编译、签名校验和本机安装完成；本机 OMP 与 MiniMax 配套文件、三个待发布包及校验值已同步
- 已在真实应用界面核对说明消失、失败状态不再显示“已就绪”；本次启动遇到钥匙串响应超时
- 本机 curl 与 Node 对正式域名的 HTTPS 检查分别出现 TLS 失败和 ECONNRESET；服务器本机 HTTPS 检查返回 200。早前成功报告仅代表当时的结果，当前公网连接仍待排查
- 本次未提交市场更新，也未将 build 28 的验证报告当作 build 29 的完整验收结果

## 2026-09-15 状态修订（build 30，待发布）

- 免费润色区分服务暂不可用、未联网、超时、钥匙串待授权和本地进程未就绪，不把服务故障描述成用户需要填写 Provider。
- 连接失败后撤销界面的已就绪状态；自动开通必须同时收到 activated 和 verified 的真实验证结果。
- MCP 首次检查明确报告 automatic_free，已保存的配置不当作当前连接成功；保留现有自定义服务。
- 新 Mac 发布门槛新增单独的 aiPolishing 实测字段，必须与当前包哈希匹配。尚未进行另一台物理 Mac 的验收，不允许以本机或隔离目录测试代替。
- 最终 build 30 已编译、验证签名并安装在本机，OMP 与 MiniMax 插件文件同步。实际应用界面显示「服务暂不可用」「无需修改配置」，旧的「AI 服务未配置」行已移除。活跃宿主 MCP 会话是否重载尚未验证。
- 14 项相关 Node 检查及原生连接错误分类测试通过；最终桌面 ZIP 的全新安装与升级保留配置检查再次通过。公网仍被阻断，当前本机诊断的麦克风及辅助功能授权均为 false，真实录音/填入需本人授权后再测。
- 完整状态与最终包哈希见 [build 30 验证记录](free-build30-verification.json)。没有提交市场更新，也没有购买香港实例或续费广州实例。
